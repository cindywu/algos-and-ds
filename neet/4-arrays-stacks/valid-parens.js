function validParens(s) {
  // const stack = [];
  // for (const c of s) {
  //     if (c === '(') {
  //         stack.push(c);
  //     } else if (c === ')') {
  //         if (stack.length === 0) {
  //             return false;
  //         }
  //         stack.pop();
  //     }
  // }
  // return stack.length === 0;
  return true
}

module.exports = validParens;